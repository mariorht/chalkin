events {
    worker_connections 1024;
}

http {
    upstream chalkin_api {
        server api:8001;
    }

    server {
        listen 80;
        server_name localhost;  # Cambiar por tu dominio en producción

        # Redirigir a HTTPS en producción (descomentar cuando tengas SSL)
        # return 301 https://$server_name$request_uri;

        location / {
            proxy_pass http://chalkin_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support (si lo necesitas)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # Caché para archivos estáticos
        location /static/ {
            proxy_pass http://chalkin_api;
            proxy_cache_valid 200 1d;
            add_header Cache-Control "public, immutable";
        }
    }

    # Servidor HTTPS (descomentar para producción con SSL)
    # server {
    #     listen 443 ssl;
    #     server_name tu-dominio.com;
    #
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://chalkin_api;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
